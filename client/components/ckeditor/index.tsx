import React, { useCallback, useState } from "react";
import axios from "axios";
import styled from "styled-components";

function EditorContainer() {
  return <>hello ckeditor</>;
}

export default EditorContainer;
